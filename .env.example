# Secret keys must stay server-side. Never expose sk_ keys in client code.
POLLINATIONS_API_KEY=sk_your_secret_key_here

# Optional. Pollinations supports OpenAI-compatible chat completions.
POLLINATIONS_MODEL=gemini-fast
